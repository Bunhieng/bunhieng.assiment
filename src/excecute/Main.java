package excecute;
import math_quize.SUM;
import util.Util;

import java.util.Scanner;

public class Main {
    public static void main(String[] args){
        int x = (int) (math.random()*10);
        int y = (int) (math.random()*10);

        SUM sumlevel1=new SUM();
        Util.showMessageCal(x, y, sign " + ");
        int studentAnser = new Scaner(System.in).nextInt();
        system.out.println(sumlevel1.sum(x, y) studentAnswer));
//          Util.showSum(x, y, sumlevel1.sum(x, y));
    }
}
